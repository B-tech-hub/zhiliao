# ADR 0021：纠正即学习

## 状态

已采用（2026-08-24）。

## 决策

- 用户通过笔记编辑界面修改主题、标题或标签时，记录为 few-shot 样例。
- AI 工具调用和撤销操作不记录样例，避免把模型行为或回滚动作当作用户偏好。
- 后续 AI 整理最多读取每类最近 3 条样例，关闭开关时完全不注入。
- 设置页允许关闭学习、查看数量和清空历史样例。

## 后果

个性化信号从锁定字段升级为可复用的监督样例，同时限制样例数量避免 Prompt 膨胀。错误样例需要通过设置页清空，暂不提供逐条编辑。
