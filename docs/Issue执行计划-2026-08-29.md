# GitHub Issue 执行计划（2026-08-29）

> 适用仓库：`B-tech-hub/zhiliao`  
> Issue 来源：<https://github.com/B-tech-hub/zhiliao/issues>  
> 调查快照：2026-08-29  
> 执行更新：2026-08-30，#11 已修复并关闭，当前进入 #6 发布准备。
> 本文只规划当前开放 Issue，不解除 README 已声明的新功能冻结。

## 1. 结论先行

调查时的 7 个开放 Issue 不应按编号逐个平铺执行。当前 #11 已完成，剩余工作继续收束成两条主线，并用一次发布把它们接起来：

1. **交付止血线**：Mermaid 资源失控问题（#11）已完成定位、修复和回归，继续完成 v0.6.0 发布（#6）。
2. **使用与分发线**：先让手机捕获真正进入日常使用（#4），再上线安全隔离的公网 demo（#5），补齐「AI 不编造」演示（#8）和 ADR 内容分发（#9）。

#10 不是开发任务，而是贯穿全程的指标闸门。真实笔记达到 100 条前，继续冻结新功能；缺陷、文档、发布、部署和分发不受影响。

建议执行顺序：

```text
#11 生产环境复现判定、修复与回归（已完成）
            ↓
#6 发布 v0.6.0
            ↓
共享基础设施决策
  ├─ #4 手机一键记录 → 连续 7 天使用验证 → #10 真实笔记数持续增长
  └─ #5 公网 demo → #8 30 秒演示 → #9 ADR 内容分发
```

关键判断：

- **#11 的发布闸门已解除。** 真实浏览器 production 未复现，headless production 稳定复现；根因是 NodeView mutation 边界，修复与回归证据已写入开发日志并同步到 GitHub Issue。
- **#6 已经部分完成。** 版本号、`CHANGELOG.md` 与 Release Notes 已准备，GitHub 最新版本仍是 v0.5.0；剩余升级彩排、RC、正式 tag 与镜像核验。
- **#4 和 #5 在实际交付上依赖 #6。** 两者需要使用 v0.6.0 才正式发布的外部捕获、功能开关和最新 demo 体验，不建议基于未发布源码长期部署。
- **#5 上公网前存在额外安全工作。** 当前 `DEMO_MODE=1` 只负责播种演示数据和显示密码提示，并不会禁止访客修改 LLM 配置、创建外部 Token、上传文件或触发其他设置操作。不能直接把现有本地 demo 原样暴露到公网。
- **#9 的流量数据必须提前留存。** GitHub Traffic 只保留有限时间窗口，不能在发文一个月后再回头获取完整的首月来源数据。

## 2. 当前状态快照

### 2.1 Issue 状态（含调查后已关闭项）

| Issue | 定位 | 当前状态 | 主要阻塞或未知项 | 建议优先级 |
|---|---|---|---|---|
| [#11](https://github.com/B-tech-hub/zhiliao/issues/11) Mermaid CPU/内存线性失控 | 缺陷、发布风险 | 已关闭 | `473d32d` 已修复；真实浏览器与 headless production 回归完成 | 已完成 |
| [#6](https://github.com/B-tech-hub/zhiliao/issues/6) 发布 v0.6.0 | 发布、交付 | `ready-for-human` | #3 已关闭；仍需验证、推送、RC、正式 tag、Release 与镜像核验 | P0 |
| [#4](https://github.com/B-tech-hub/zhiliao/issues/4) 手机一键记录 | 使用增长根因项 | `enhancement`、`ready-for-human` | 正式实例远程可达、手机平台、Token 和快捷指令模板 | P0 |
| [#5](https://github.com/B-tech-hub/zhiliao/issues/5) 公网 demo | 分发基础设施 | `enhancement`、`ready-for-human` | 服务器、域名、HTTPS、demo 安全边界、重置机制 | P0 |
| [#8](https://github.com/B-tech-hub/zhiliao/issues/8) 「AI 不编造」演示 | 产品证明、文档 | `documentation`、`ready-for-human` | 可复现问题与素材、公网 demo 最终落点 | P1 |
| [#9](https://github.com/B-tech-hub/zhiliao/issues/9) ADR 系列文章 | 内容分发 | `documentation`、`ready-for-human` | 成文、人工发布、流量快照机制 | P1 |
| [#10](https://github.com/B-tech-hub/zhiliao/issues/10) 新功能冻结闸门 | 指标治理 | `needs-triage` | #4 是增长前提；真实笔记数尚未达到 100 | P2，持续跟踪 |

### 2.2 已完成的前置工作

- #3「四项非核心功能默认关闭」已在提交 `4627c3e` 落地并关闭，#6 的显式阻塞已解除。
- #11 已完成复现矩阵、修复、回归记录与 GitHub Issue 收尾，发布阻塞解除。
- #6 的版本号、`CHANGELOG.md` 与 Release Notes 已准备完成；Mermaid 修复基线提交为 `473d32d`。
- `POST /api/external/capture`、`capture:write` Token、Token 吊销已经实现，#4 原则上不需要新增服务端业务接口。
- `docker-compose.demo.yml`、mock LLM、演示数据播种和本地 `npm run demo` 已存在，#5 的核心演示链路已具备。
- README 已声明真实笔记数为北极星指标，并在 100 条前冻结新功能。

### 2.3 当前仓库状态

- 当前分支：`main`
- Mermaid 修复基线：`473d32d fix: 修复 Mermaid headless 渲染循环`
- 远端 `origin/main`：`473d32d`；本地有待提交的发布材料改动
- GitHub 最新 Release：v0.5.0
- `473d32d` 的 CI 已通过设计门禁、ESLint、Vitest 和 Next.js 构建
- Release 工作流会在推送版本 tag 后构建 amd64/arm64 镜像并创建 GitHub Release

## 3. 执行原则与边界

### 3.1 不变量

- 真实笔记数未达到 100 前，不实施 #10 中冻结的六个新功能方向。
- 缺陷修复、文档、分发、发布和运维可以继续。
- 正式库、演示库、测试库必须使用不同数据库、上传目录、容器项目名和密钥。
- 所有公网入口默认按不可信流量处理，不能因为使用共享密码就视为可信环境。
- tag、GitHub Release、DNS、服务器部署、对外发文等外向操作保留人工确认。
- 每个 Issue 完成时同步更新相关 README、专项文档、CHANGELOG 或 Issue 评论，避免知识只留在执行会话中。

### 3.2 建议的工作批次

| 批次 | 内容 | 预计时间盒 | 退出条件 |
|---|---|---:|---|
| A | #11 复现判定与必要修复 | 0.5 至 1.5 天 | 真实浏览器/生产构建结论明确，若修复则回归稳定 |
| B | #6 v0.6.0 发布 | 0.5 至 1 天，CI 等待另计 | Release、镜像、升级路径均验证 |
| C | #4 手机一键记录 | 技术配置 0.5 至 1 天，行为验证 7 天 | 连续 7 天每天至少一条真实捕获 |
| D | #5 公网 demo | 1 至 2 天 | HTTPS 可访问、安全边界与自动重置有效 |
| E | #8 30 秒演示 | 0.5 至 1 天 | README 中可看、demo 中可复现 |
| F | #9 ADR 首篇分发 | 写作 1 至 2 天，观察 30 天 | 至少一篇投出，流量和转化按节点留存 |
| G | #10 指标闸门 | 持续 | 真实笔记数达到 100 后重新分诊 |

时间盒是控制投入的上限，不是承诺日期。#4 与 #9 都包含必须跨天观察的真实信号，不能用一次技术验收代替。

## 4. 批次 A：#11 Mermaid 性能缺陷

### 4.1 目标

回答两个问题：

1. 问题是否在真实桌面浏览器和生产构建中复现？
2. 若复现，究竟是 Mermaid 自身、TipTap NodeView 更新、主题观察器，还是临时 DOM 清理造成的循环？

### 4.2 已知证据

- 单条笔记、单个两节点图即可触发。
- headless Chrome + swiftshader + `npm run dev` 下 CPU 与内存线性增长。
- 在 #3 前后的提交上表现一致，可排除功能开关改动直接引入回归。
- `src/components/mermaid-code-block.tsx` 中存在三个高价值假设：
  - `mermaid.render()` 的临时 DOM 仅在异常分支显式清理。
  - `MutationObserver` 对根元素 `class` 的变化强制重渲染，并绕过去重判断。
  - NodeView `update()`、异步渲染完成后的 `applyMode()` 可能形成重复更新。

### 4.3 复现矩阵

使用同一份隔离数据、同一个 Mermaid 代码块，按以下顺序执行。每轮最多观察 5 分钟，发现线性失控立即终止标签页，不做长时间放任。

| 构建 | 浏览器 | 图形环境 | 目的 |
|---|---|---|---|
| dev | 真实 Chrome/Edge | 硬件加速 | 判断日常开发是否复现 |
| production | 真实 Chrome/Edge | 硬件加速 | 决定是否阻塞发布 |
| production | headless Chrome | 默认 headless 图形栈 | 判断是否为 headless 特有 |
| 最小 HTML | 真实浏览器 | 直接调用 `mermaid.render` | 区分 Mermaid 与 TipTap 集成问题 |

每轮记录：

- 浏览器版本、Node.js 版本、构建模式、是否硬件加速。
- 首次渲染次数、30 秒和 5 分钟的 CPU/内存。
- 页面中 Mermaid 临时节点数量。
- 根元素 `class` 变更次数。
- NodeView `update()`、`renderDiagram()`、`applyMode()` 调用次数。

### 4.4 决策规则

- **生产构建 + 真实浏览器复现**：视为发布阻塞缺陷，完成修复后再进入 #6。
- **仅 dev 模式复现**：继续定位开发模式触发条件；若生产稳定，在 #11 写清边界并降低发布阻塞级别。
- **仅 headless/软件渲染复现**：建立最小复现并记录测试环境限制；不为了 headless 特性盲改生产路径。
- **任何环境都无法复现**：保留原始数据和版本信息，在 Issue 中记录无法复现条件，移除 `needs-info` 前先补齐人工真实浏览器结论。

### 4.5 修复策略候选

仅在证据命中后采用对应改动，不一次性把所有假设都改掉：

1. 无论成功或失败，都在 `finally` 清理 Mermaid 为本次渲染创建的临时 DOM。
2. 主题观察器记录上一次解析后的主题，只在 `dark` 与 `neutral` 真正变化时重渲染。
3. 不以 `force=true` 无条件绕过去重；主题未变且源码未变时必须直接返回。
4. 对并发渲染保留版本号取消机制，旧 Promise 完成后不得覆盖新结果。
5. 若 `mermaid.initialize()` 被证明参与循环，改为受控初始化，不在每次普通更新时重复执行。
6. 若问题来自 NodeView DOM 更新，缩小 `applyMode()` 的变更范围，避免对 ProseMirror 观察范围制造无意义 mutation。

### 4.6 验证与文档

局部验证优先：

- 为去重、主题变化、销毁后异步返回、临时 DOM 清理补组件或 DOM 单元测试。
- 使用一个合法图和一个非法图验证成功、失败、源码回退、明暗切换。
- 生产构建真实浏览器观察 5 分钟：预热后内存不再线性增长，CPU 回落到空闲水平，渲染次数稳定。
- 验证编辑源码、点击图回到源码、保存后 Markdown 围栏不变。

文档同步：

- 若确认并修复，在 `CHANGELOG.md` v0.6.0 修复项中补充。
- 在 `docs/开发日志.md` 记录根因、误导性假设与真实性能基线。
- 只有形成新的长期架构约束时才新增 ADR；普通内存泄漏修复不需要为了留痕硬写 ADR。
- 在 #11 评论中附复现矩阵和修复前后数据，再关闭 Issue。

## 5. 批次 B：#6 发布 v0.6.0

### 5.1 当前已完成

- `package.json` 和 `package-lock.json` 已升至 `0.6.0`。
- `CHANGELOG.md` 已将「未发布」整理为 `0.6.0 - 2026-08-30`。
- 已新增 `docs/releases/v0.6.0.md`，Release 工作流通过 `body_path` 使用该正文。
- #3 已关闭，原始显式阻塞解除。

### 5.2 发布前必须解决的缺口

1. **升级彩排**：必须从真实 v0.5.0 镜像升级到候选版本，而不是只在全新数据库上启动。
2. **发布材料提交**：将 Release Notes、工作流与 CHANGELOG 收尾改动提交并推送到 `main`，等待新提交 CI 全绿。
3. **Issue 收尾**：#6 发布完成后再关闭。

### 5.3 升级彩排

在隔离 compose project 与隔离命名卷中执行：

1. 启动 v0.5.0，创建多条笔记、主题、标签、图片和至少一条 Mermaid 代码块。
2. 配置一次 Embedding 并生成存量向量；设置页保存一组会覆盖环境变量的模型配置。
3. 备份数据库与上传目录，记录升级前计数。
4. 切换到 v0.6.0 候选镜像并启动，确认迁移自动完成。
5. 验证笔记、主题、标签、图片、会话、Token、导出文件均未丢失。
6. 验证存量向量显示为需要补算，补算后搜索恢复。
7. 验证四项非核心功能升级后默认关闭，但历史内容仍可显示、编辑和导出。
8. 验证设置页明确显示数据库配置覆盖环境变量的情况。
9. 执行一次回滚演练说明：仅验证备份可恢复，不直接拿正式库试回滚。

### 5.4 发布流水线

正式执行前先征得维护者确认，再运行重验证：

```bash
npm run check:design
npm run lint
npm test
npm run build
```

随后按顺序执行：

1. 提交并推送发布材料，等待 `main` CI 全绿。
2. 完成隔离升级彩排并记录结果。
3. 在同一提交上打 `v0.6.0-rc1` 并推送。
4. 等待 amd64/arm64 镜像、manifest、预发布 Release 全绿。
5. 拉取 RC 镜像做一次最短冒烟：登录、新建笔记、AI 整理、搜索、导出、外部捕获。
6. 不再夹带提交，在同一 commit 上打 `v0.6.0`。
7. 检查 GitHub Release 正文、CHANGELOG 链接和升级说明。
8. 检查 `0.6.0`、`0.6`、`latest` 指向同一多架构 manifest。
9. 从匿名环境执行一次 `docker pull`，确认包仍为 public。
10. 在 #6 评论中逐条勾选验收项后关闭。

### 5.5 失败与回退条件

- 任一平台镜像失败，不推正式 tag。
- RC 与正式 tag 之间出现代码变化，重新打新的 RC，不复用旧彩排结论。
- 升级彩排出现数据不一致，停止发布，先恢复隔离备份并修迁移或升级逻辑。
- Release Notes 未包含向量补算、配置覆盖、四项功能默认关闭，不发布正式版本。

## 6. 共享基础设施决策

#4 与 #5 可以共用一台常驻机器，但不能共用安全边界。推荐拓扑：

```text
手机快捷指令
  → Tailscale HTTPS
  → zhiliao-prod（真实配置、真实数据，仅私网）

公网访客
  → 公网域名与 HTTPS 反向代理
  → zhiliao-demo（mock LLM、演示数据、受限权限、定时重置）
```

### 6.1 必须由维护者确定的事项

- 正式实例运行位置：家中常开设备、NAS 或云服务器。
- 手机主平台：iOS 快捷指令或 Android 等价工具。首轮只要求一个主平台完整验收。
- 公网 demo 使用的域名、DNS 与服务器。
- 是否让正式实例与 demo 共机。若共机，必须使用独立 compose project、网络、卷和环境文件。

### 6.2 推荐隔离规则

- 正式项目名固定为 `zhiliao-prod`，demo 固定为 `zhiliao-demo`。
- demo 不挂载正式目录，不读取正式 `.env`，不共享 Docker volume。
- 正式实例只经 Tailscale 暴露，不向公网开放登录页。
- 公网反向代理只转发 demo 容器。
- demo 容器禁止访问云元数据地址和内网网段，能做出站白名单时只允许必要依赖。
- 为 demo 设置 CPU、内存、磁盘和请求体上限，避免公开上传接口耗尽宿主机。

## 7. 批次 C：#4 手机一键记录

### 7.1 目标与范围

从手机主屏、控制中心或分享菜单一步写入正式库，并走完现有 AI 整理队列。首期不改服务端接口：

```http
POST /api/external/capture
Authorization: Bearer <capture:write token>
Content-Type: application/json

{
  "content": "要记录的内容",
  "topicId": "可选主题 ID"
}
```

成功响应为 HTTP 201；空内容返回 400；Token 无效或权限不符返回 401。

### 7.2 实施步骤

1. 完成 v0.6.0 发布并升级正式实例。
2. 通过现有 Tailscale 手册让正式实例在蜂窝网络下也可使用 HTTPS 访问。
3. 在设置页创建一个专用 `capture:write` Token，不复用 `knowledge:read` Token。
4. 制作「分享文本」入口：接收系统分享的文本或网址，组装 JSON 后调用捕获接口。
5. 制作「语音听写」入口：先听写，再让用户快速确认文本，随后发送。
6. 对 201、400、401、超时和网络断开给出明确通知，失败时保留原文本以便重试。
7. 在设置页吊销 Token，确认旧快捷指令立即返回 401；再生成新 Token 更新快捷指令。
8. 检查写入后的笔记进入 AI 队列，最终得到标题、标签、摘要和主题。
9. 开始连续 7 天真实使用观察，不以测试笔记充数。

### 7.3 快捷指令安全要求

- Token 只保存在快捷指令本地，不写入截图、README、日志和共享模板。
- 对外发布的模板必须使用占位符，导入后由用户自行粘贴 Token 和实例地址。
- 正式 Token 只授予 `capture:write`，即使泄露也不能读取知识库。
- 文档明确说明 Token 丢失无法找回，只能吊销后重建。
- 分享网址时默认记录 URL 与用户选中文本，不在快捷指令里增加未经讨论的网页抓取能力。

### 7.4 文档交付

建议新增 `docs/手机快捷记录.md`，并从 README、README.en.md 和文档索引链接。内容至少包括：

- 正式实例 HTTPS 前置条件。
- Token 创建与吊销步骤。
- iOS 或 Android 主平台的逐步配置图文。
- 可导入模板或官方分享链接。
- 接口请求和错误码。
- 常见故障：401、超时、证书不受信任、蜂窝网络不可达、AI 一直待整理。

若接口契约因此变化，必须同步 ADR 0019 和接口文档；若只增加客户端模板，不改 ADR。

### 7.5 验收

- 手机主屏或控制中心一步进入记录流程，不先打开浏览器登录。
- 分享文本和语音听写两种入口均成功。
- 仅使用蜂窝数据时成功写入。
- Token 吊销后立即失效。
- 写入内容走完 AI 整理。
- 连续 7 天每天至少一条真实笔记来自该入口。

若技术项全部通过但 7 天行为信号失败，不关闭 #4。重新做五问溯因，判断摩擦来自入口层级、听写确认、网络等待、内容组织焦虑或其他环节。

## 8. 批次 D：#5 公网 demo

### 8.1 上线前安全缺口

现有 `DEMO_MODE` 不能直接视为公网沙箱。当前它主要做两件事：空库播种演示数据、登录页提示密码。共享密码登录后，访客仍可能：

- 修改 LLM、Embedding、视觉、推理或图像模型配置。
- 创建 `capture:write` 或 `knowledge:read` Token。
- 上传图片，占用磁盘。
- 修改或删除演示数据，使后续访客看到失真状态。
- 触发备份、导入、重算等高成本操作。

因此，公网部署前需要一个小型「demo 安全化」子任务。它属于部署安全与既有功能保护，不视为冻结中的新功能。

### 8.2 服务端 demo 边界

建议在服务端统一识别公开演示模式，并至少执行：

1. 禁止保存或清除任何外部模型配置，demo 永远只使用容器内 mock LLM。
2. 禁止创建外部 API Token，避免访客获得长期写入或读取入口。
3. 禁止备份下载、数据导入、向量补算等不属于演示主线的操作。
4. 评估是否完全禁止图片上传；若保留，降低单文件上限并配合代理层总请求体限制。
5. 保留新建普通文本笔记、AI 自动归档和主题建议采纳，因为它们是验收主路径。
6. 前端隐藏已被服务端禁止的设置入口，但不能只依赖隐藏按钮。
7. 为被禁止的 API 增加 demo 模式测试，直接请求时返回明确的 403。

### 8.3 部署与运维

1. 使用 v0.6.0 固定版本镜像部署，避免 `latest` 漂移导致演示内容未经验证变化。
2. 使用独立命名卷和独立容器网络，不挂正式数据目录。
3. 配置域名与 HTTPS，HTTP 自动跳转 HTTPS。
4. 代理层增加请求速率限制、请求体限制和基本恶意流量防护。
5. 容器配置 CPU、内存与日志轮转上限。
6. 设定定时重置，例如每天凌晨销毁 demo 卷并重建，让播种逻辑恢复标准数据。
7. 重置后自动执行健康检查和三步主路径冒烟。
8. 配置最小监控：可用性、磁盘、容器重启次数、5xx 比例、证书到期。
9. 保存一份手动重置命令，定时任务失败时可以人工恢复。

### 8.4 README 与文档

- README.md 与 README.en.md 徽章下方、正文之前放 Live Demo 链接和密码。
- 明确说明演示数据会定时重置，不要输入隐私或真实密钥。
- 新增 `docs/公网演示部署.md`，记录拓扑、环境变量、代理、重置、监控、故障恢复和安全限制。
- `docker-compose.demo.yml` 的注释与实际部署方式保持一致。

### 8.5 验收

- 公网 HTTPS 可访问，证书可信。
- 演示容器不持有真实 API Key，访客也无法写入外部模型配置。
- 访客无法创建外部 Token。
- 正式数据、正式卷、正式环境文件与 demo 完全隔离。
- 新建笔记、AI 自动归档、主题建议采纳三步走通。
- 自动重置和手动重置均成功。
- 资源限制与磁盘保护有效。
- README 中英文首屏链接正确。

## 9. 批次 E：#8 「AI 不编造」30 秒演示

### 9.1 演示设计

演示核心不是证明某个通用模型一定会幻觉，而是让访客看到知了的严格接地边界可被复现。

建议分镜：

1. 左侧展示对照助手回答一个知识库里明确不存在的问题。
2. 右侧在知了的来源问答中提出完全相同的问题。
3. 知了明确回答「来源笔记中没有相关内容」。
4. 快速展示来源集，证明答案边界来自已选择的笔记，而非剪辑隐藏。
5. 结尾显示公网 demo 链接，访客可自行复现。

总时长控制在 20 至 30 秒，不依赖旁白。画面文字必须在 README 常见宽度下可读。

### 9.2 可复现性要求

- 固定并记录演示数据版本、来源笔记列表、问题文本、对照模型名称与日期。
- 问题必须确实不在来源笔记中，可通过文本搜索和人工核对证明。
- 若对照助手当次没有编造，如实更换问题并重新录制，不剪掉不利结果拼接结论。
- 公网 demo 重置后应恢复到同一套可复现数据。
- 演示素材不得包含真实个人笔记、真实 Token 或真实 API Key。

### 9.3 产物与同步

- 更新 `docs/演示GIF录制清单.md`，增加严格接地对比演示的分镜和素材清单。
- 生成体积可控的 GIF 或短视频，并提供静态封面和准确的替代文本。
- README.md 与 README.en.md 在「AI 不编造」承诺附近同步展示。
- 若演示依赖新增固定数据，更新 demo 播种脚本和相应测试。

### 9.4 验收

- 30 秒内，无旁白也能理解差异。
- README 首屏附近可直接看到。
- 问题与演示数据被文档化。
- 公网 demo 中可以重复得到知了的严格接地回答。
- 对照展示不做误导性剪辑。

## 10. 批次 F：#9 ADR 内容分发

### 10.1 发布时机

建议在 v0.6.0 发布、README Live Demo 可用后投出首篇。这样文章中的仓库链接指向已发布版本，读者也有立即体验的出口。

### 10.2 首篇建议主题

优先使用「设置页配置静默覆盖环境变量，导致 4B 模型从未真正生效」作为主线，因为它具备完整故事结构：

1. 表面现象：配置文件明明改了，行为却没有变化。
2. 潜伏原因：数据库配置优先且界面没有暴露覆盖关系。
3. 实际损失：向量按错误模型重算。
4. 为什么普通测试没发现。
5. 修复：让覆盖关系显式可见，并补防复发验证。
6. 方法论：AI 辅助开发中，配置来源优先级必须成为可观察状态。

第二篇再写「验收判据被真实数据推翻」，避免首篇同时塞入过多故事。

### 10.3 写作要求

- 以决策、证据、错误判断和自我推翻为主线，不写成功学式项目介绍。
- 所有关键结论链接到具体 ADR、开发日志、提交或固化测试。
- 清楚区分当时已知、事后发现和仍未知的部分。
- 中文渠道分别适配标题和开头，不机械复制同一版式。
- Hacker News 使用英文摘要与仓库链接，正文可放英文版长文或项目内英文文档。

### 10.4 流量留存计划

GitHub Traffic 不能等一个月后再一次性补采。发文前建立基线，随后定期保存：

| 时间点 | 记录内容 |
|---|---|
| 发布前 | star、watcher、open issue、views、unique visitors、clones、referrers |
| 24 小时 | 同上，记录各渠道首波流量 |
| 7 天 | 同上，判断短期转化 |
| 14 天 | 保存 GitHub 可见窗口内的完整来源快照 |
| 30 天 | star、issue、实际用户反馈与 demo 使用情况；结合已保存快照做结论 |

建议将快照保存为 `docs/分发数据/` 下的日期化 Markdown 或 CSV，字段固定，避免只留截图。

### 10.5 失败条件

沿用 Issue 的预设条件：首篇发出 30 天后，若 star 仍低于 20 且 issue 仍为 0，则不继续用相同形式批量发文。复盘时区分：

- 内容没有曝光。
- 有曝光但标题与开头没有转化。
- 有仓库访问但 demo 或 README 没有承接。
- 有体验但产品本身没有形成使用意愿。

不同原因对应不同动作，不能统一归因成「再多发几篇」。

## 11. 批次 G：#10 新功能冻结闸门

### 11.1 指标口径

在持续跟踪前先固定「真实笔记数」定义，建议：

- 计入：用户真实撰写、手机捕获、从个人历史资料导入的有效笔记。
- 不计入：demo 数据、自动化测试数据、专门为验收临时创建的样本。
- 每周回顾等自动生成笔记单独记录，不作为捕获能力成功的主要证据。
- 删除进回收站的无效测试笔记不计入。

建议同时记录两个辅助指标：

- `capture:write` 入口带来的真实笔记数。
- 过去 7 天有真实笔记写入的活跃天数。

### 11.2 跟踪节奏

- #4 启用后的前 7 天每日记录一次。
- 之后每周记录一次真实笔记总数和各入口增量。
- 达到 50 条时只做一次体验复盘，不自动解冻功能。
- 达到 100 条后关闭冻结闸门，重新分诊六个候选方向，仍需逐项立 Issue 和排序。

### 11.3 冷启动模式的例外处理

#10 将「冷启动模式」列为最可能提前解冻的方向，但不得直接开发。只有出现以下证据时才提请提前解冻：

- 小库存下的聚类、主题建议或每周回顾持续制造明确困扰。
- 困扰直接影响 #4 的连续使用信号。
- 关闭或隐藏现有能力比继续教育用户成本更低。

满足证据后，在 #10 中先做人工决策，再另开缺陷或体验修复 Issue；不能借「可提前」绕开闸门。

## 12. 文档同步矩阵

| Issue | 必须同步的文档 |
|---|---|
| #11 | `CHANGELOG.md`、`docs/开发日志.md`、Issue 复现与回归数据；必要时 ADR |
| #6 | `CHANGELOG.md`、版本 Release Notes、升级说明、发布 Issue 评论 |
| #4 | 新增手机快捷记录指南、README 中英文、接口契约；接口边界变化时更新 ADR 0019 |
| #5 | 新增公网 demo 部署与运维文档、README 中英文、compose 注释、安全限制 |
| #8 | README 中英文、演示录制清单、固定演示数据说明 |
| #9 | 发布文章、ADR/日志链接、分发数据快照 |
| #10 | README Roadmap、Issue 指标评论；解冻时补新的排序决策 |

## 13. 人工与 Agent 分工

| 工作 | Agent 可完成 | 必须人工参与 |
|---|---|---|
| #11 代码调查、埋点、修复、测试草稿 | 是 | 真实桌面浏览器任务管理器观察建议人工复核 |
| 本地检查、升级彩排脚本与 Release Notes | 是 | 是否运行全量测试、推送 main、打 tag、发 Release |
| 快捷指令说明与模板占位版 | 是 | 在真实手机导入、粘贴 Token、蜂窝网络连续 7 天使用 |
| demo 安全化代码与部署文档 | 是 | 服务器、域名、DNS、证书和费用决策 |
| GIF 分镜、素材清单、README 修改 | 是 | 对照模型录制与最终素材真实性确认 |
| ADR 文章草稿与数据采集脚本 | 是 | 各平台账号发布、评论互动、30 天结论 |

## 14. 风险清单

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| #11 只在 headless/dev 复现 | 为测试环境过度改动生产代码 | 先跑复现矩阵，命中证据后再改 |
| v0.6.0 Release 正文未使用已写草稿 | 对外版本说明不完整 | 发布前确定 `body_path` 或人工替换流程 |
| 正式实例与 demo 共机误共享数据 | 真实笔记或密钥泄露 | 独立项目名、卷、网络、环境文件；demo 不挂正式目录 |
| 公网 demo 可修改模型设置 | 产生外部请求、滥用费用或 SSRF 风险 | 服务端 demo 模式 403、隐藏入口、出站限制 |
| 公网上传耗尽磁盘 | demo 与正式服务同时受影响 | 禁止或收紧上传、请求体限制、磁盘配额、定时重置 |
| 快捷指令 Token 泄露 | 任意人可向正式库写垃圾内容 | 最小权限、专用 Token、即时吊销、模板不含真 Token |
| 技术验收通过但 #4 没形成习惯 | 真实笔记数继续停滞 | 7 天行为验收作为关闭条件，失败重新溯因 |
| 发文后未及时保存 Traffic | 无法判断渠道效果 | 发布前、1 天、7 天、14 天、30 天定点快照 |
| 同时推进过多外向任务 | 发布、部署、写作都半成品 | 严格按批次退出条件推进，一次只开一个主执行批次 |

## 15. 总体验收标准

本轮计划完成，不等于所有 Issue 同一天关闭。最终应达到：

- #11 在真实浏览器和生产构建下有明确结论；若复现，已有修复和回归证据。
- v0.6.0 已发布，升级路径、Release Notes 和多架构镜像经过验证。
- 手机快捷记录在蜂窝网络下可用，并连续 7 天产生真实笔记。
- 公网 demo 使用 HTTPS、与正式数据隔离、无法写入外部模型配置或创建 Token、可自动重置。
- 「AI 不编造」演示可在 README 观看，并能在 demo 中复现。
- 至少一篇 ADR 方法论文章投出，渠道数据按时间点留存。
- #10 持续记录真实笔记数，未达到 100 前没有偷偷恢复新功能开发。

## 16. 建议的下一步

下一执行动作应是 **提交发布材料、完成 v0.5.0 → v0.6.0 隔离升级彩排**，随后进入 RC 发布流程，不建议跳过候选版本验证，因为：

- v0.6.0 是 #4、#5 对外使用最新能力的稳定基线。
- #11 若确实影响生产，发布后再修会立刻产生 v0.6.1 的额外交付成本。
- #5 和 #9 的对外链接应指向已经发布且可验证的版本，而不是本地领先远端的提交。
