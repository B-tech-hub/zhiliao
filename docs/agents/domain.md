# 领域文档使用规则

本仓库采用单一领域上下文。

## 调查前读取

- 读取根目录 `CONTEXT.md`，了解项目领域术语。
- 读取 `docs/adr/` 中与当前改动相关的架构决策。
- 若文件不存在则继续工作，不为补齐形式而预先创建领域文档。

## 使用领域语言

任务说明、测试名称、实现方案和审查结论应使用 `CONTEXT.md` 定义的词汇，避免为已有概念另造同义词。确有新概念时，先判断它是领域缺口还是不必要的新术语。

## 处理 ADR 冲突

方案若与既有 ADR 冲突，必须明确指出冲突的 ADR、冲突原因以及重新讨论该决策的依据，不得静默覆盖。
