# 项目协作说明

## Agent skills

### Issue tracker

项目任务与 PRD 记录在 GitHub Issues，使用 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix` 五类标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单一领域上下文：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
