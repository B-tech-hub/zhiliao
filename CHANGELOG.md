# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- AI 助手的完整文字回答可直接「存为新笔记」，视觉识别结果无需复制粘贴。
- 图片上传上限提升至 20 MB，按文件魔数识别 PNG、JPEG、GIF、WebP 与 HEIC；HEIC 保留原件并生成 JPEG 展示副本。
- 笔记详情页增加桌面宽屏画布及 H1–H3 标题目录，移动端保持单栏。
- AI 助手增加默认关闭、不持久化的「深度思考」消息级开关和独立推理模型配置。
- **深度思考显示思考过程**：解析 `reasoning_content`、`reasoning` 与内联 `<think>…</think>` 三种线格式，随消息落库，在回答上方以折叠栏呈现——模型思考时自动展开，正文开始输出时自动收起，刷新后仍可查看。思考过程不回灌进后续请求的上下文，「存为新笔记」时也不带上（取舍见 [ADR-0015](docs/adr/0015-deep-thinking-tools-and-trace.md)）。
- 设置页「深度思考模型」增加「测试连接」：连通性、是否外露思考过程、是否支持工具调用一次测完，工具能力结论单独记为 `reasoning_supports_tools`。
- **共享正文排版层**：新增 `.prose-note`，笔记编辑器与助手回答共用同一套段落间距、标题留白、列表节奏、等宽代码块与行宽上限（约 40 字/行）。颜色圆角全部取既有设计 token，暗色自动跟随（取舍见 [ADR-0016](docs/adr/0016-shared-prose-layer.md)）。
- **助手回答渲染 Markdown**：标题、列表、表格、代码块、引用块正常成形，回答不再套气泡而是占满面板宽度的文档式排版。引用标记 `[^id]` 在解析前转成站内链接，避免被 GFM 当成脚注吞掉；模型编造的 id 仍保持纯文本。
- **助手面板可拖宽**：默认宽度从 420px 提到 560px，拖左边缘可在 420–900px 之间调整，宽度记在 `localStorage`；移动端仍是全宽，不显示把手。
- 列表密度提升：笔记卡从 24px 内边距的大卡改为紧凑行、摘要压到单行，一屏可见条数约翻倍；首页主题卡去掉固定高度与 40px 巨大数字，宽屏下每行 4 个。
- **编辑器斜杠命令**：正文里输入 `/` 弹出命令菜单，支持标题、列表、引用、代码块、Mermaid 图、表格、分隔线，可用中文或 `h1`/`table`/`mermaid` 等别名过滤，上下键与回车选取。`and/or`、`3/4` 这类正常输入不会误触发。
- 笔记目录高亮当前阅读位置，随正文滚动跟随。
- 笔记详情页「专注」模式：收起工具栏、目录与标签行，只留标题与正文。

### 修复

- **开着「深度思考」时助手声称「不能生图」**：此前推理请求会把整个工具集清空，`generate_image`、笔记检索与写入全部不下发，深度思考实际退化成不认识知识库的纯问答。现改为照常下发全部工具，能否使用由深度思考模型自己那份探测结论决定；仅删除需确认、来源问答禁用生图与抓网页、每条消息最多 2 张图等既有防线原样生效。
- **笔记正文段落、标题、列表之间没有任何间距**：Tailwind Preflight 清零了这些元素的 margin，而项目从未补回，正文因此贴死成一坨、标题只是「变大变粗的一行字」；代码块也一直没有等宽字体。
- 笔记详情页正文过长时溢出卡片并压住底部标签行：编辑器外层容器不是 flex 列，导致编辑器的限高失效。
- 未配置深度思考模型时，「深度思考」开关不再照常亮着——改为置灰并注明「（未配置）」，此前点击静默无效。
- 设置页的「深度思考模型」配置块此前被误放在「账号」区，现归回「AI 服务」区。
- AI 看图不再把多张原始图片直接 Base64 塞入请求；改为生成不落盘的高质量 WebP 副本，并按图片数量、单图字节与总字节设限，避免大请求触发上游网站防火墙。上游返回 HTML 错误页时只展示简洁诊断，不再把整段页面输出到聊天界面。

## [0.5.0] - 2026-08-17

### 新增

- **AI 画图**：助手第 9 个工具 `generate_image`，生成结果以卡片呈现，可「插入当前笔记」或「存为新笔记」，模型也可自行调 `append_to_note`。只做 OpenAI 兼容的同步接口（`url` 与 `b64_json` 两种返回一律当场取字节落盘，图片类型按魔数嗅探）；**不设确认卡片**，改以每条用户消息最多 2 张封顶且失败也计数；来源问答中禁用（取舍见 [ADR-0011](docs/adr/0011-image-generation.md)）
- 设置页第三组「图像模型」（`image_base_url` / `image_api_key` / `image_model`，回落语义同视觉模型）；「测试连接」会真实生成一张最小尺寸图，按钮旁注明消耗一次额度
- 新端点 `POST /api/notes/[id]/append`（服务端读改写，避免客户端覆盖竞态）
- **Mermaid 图表**：笔记编辑器里语言标为 `mermaid` 的代码块渲染成图，光标移入变回源码、移开重新出图，渲染失败显示源码与错误行；跟随深浅色重渲染。存储与导出形态不变，仍是原生 Markdown 代码块（Obsidian 直接可读）；mermaid 走动态 import，不进首屏 bundle
- **每周回顾**：每周一凌晨把上一自然周新建的笔记梳理成一篇回顾，存入自动创建的「每周回顾」主题（`aiStatus=skipped` 且主题/标题双锁，不再被 AI 流水线整理）。默认开启，设置页可关，另有「立即生成上周回顾」手动补；空周不产报告（取舍见 [ADR-0012](docs/adr/0012-weekly-review.md)）
- 新端点 `/api/review`：`PATCH` 开关、`POST` 手动入队
- 环境变量新增 `IMAGE_BASE_URL` / `IMAGE_API_KEY` / `IMAGE_MODEL` / `IMAGE_TIMEOUT_MS`（默认 180 秒）与 `TZ`

### 变更

- **聊天界面重设计**：一体化输入卡（多行自增高输入 + chips 控件行 + 发送 pill），上下文附件、来源集、读图开关从顶部横条收进输入卡 chips；空态按会话形态给三套文案与建议动作 chips；气泡与卡片圆角统一为 18px
- 助手提示词补充：写笔记涉及流程/结构/时间线时用 mermaid 代码块，聊天回答里不输出（那里不渲染）
- AI 任务队列遇到未知任务类型改为显式抛错并重试，不再静默标记完成——新增任务类型忘了接线时不会再无声无息地消失

### 修复

- SSE 流未收到结束事件就断开时（后端进程被杀、反代超时），气泡尾部显示「回答可能不完整」；用户主动点停止或流内已报错时不提示
- 每周回顾的周界改按任务入队时刻计算：任务因停机、退避重试或「LLM 未配置」延后到跨周才执行时，原先会按执行时刻现算，导致原定那一周永远补不回来（对表已记为「已安排」），而新一周生成两篇

## [0.4.0] - 2026-08-15

### 新增

- **来源问答**：先勾选若干笔记与主题作为来源集，整场对话严格接地——AI 只依据来源内容回答，来源里没有的直接说没有，用户在对话中要求破例也不放行（设计取舍见 [ADR-0010](docs/adr/0010-grounded-source-chat.md)）
- 来源集挂在会话上（新表 `conversation_sources`，迁移 `0007`），会话进行中可随时增删来源，下一条消息生效；主题作为来源是**活引用**，之后新增到该主题的笔记自动进入来源范围
- 笔记页与主题页新增「以此为来源提问」，一步开启以当前对象为唯一来源的问答
- 会话作用域新增 `sources`；会话列表中来源问答标注「来源问答（N 个来源）」
- 新端点 `PUT /api/chat/conversations/[id]/sources`（覆盖式更新来源集）；`GET /api/chat/conversations/[id]` 增加 `sources` 与 `sourceNoteIds` 字段
- SSE 事件新增 `{grounding}`：开流前回报本轮生效的来源笔记，供引用溯源放行来源笔记（它们的正文直接进上下文，没经过工具）

### 变更

- 来源问答模式下禁用 `fetch_url`（外部网页不属于来源），`search_notes` / `read_note` 限定在来源集范围内，执行层二次校验，模型硬调也会被拒
- 来源全文超过 12000 字符时改为注入来源清单，正文交给限域工具按需取；此时界面明确提示「来源集过大」，模型不支持工具调用时提示正文不可见

### 修复

- 续聊时上下文取自当前页面而非会话自身作用域：从历史里打开一场会话再发言，注入的是当前页的笔记/主题。今天只是上下文错位，有了来源问答后会静默丢掉整个来源集。现改为会话已存在时一律以其自身作用域为准
- 每日清扫的孤儿会话判定改为显式列举，**未知作用域默认存活**：原先未知取值一律当主题查、查不到即删，`sources` 落进去就是 v0.3「全局会话被删光」那次事故的完整复刻

## [0.3.1] - 2026-08-15

### 修复

- 容器健康检查始终为 unhealthy：镜像缺少 `HOSTNAME=0.0.0.0`，Next standalone 因此绑定到 Docker 默认设置的容器 ID 主机名，容器内 `127.0.0.1` 不可达，而 HEALTHCHECK 探的正是它。服务对外一直正常，只是探针连不上自己——该缺陷自 v0.1.1 引入健康检查起就存在

## [0.3.0] - 2026-08-15

### 新增

- AI 助手工具调用：对话中可检索笔记、读取全文、列出主题、新建笔记、追加内容、修改分类标签、删除笔记、抓取网页共 8 个工具。删除前必须由用户确认，其余写操作直接执行并可逐条撤销
- 撤销与确认两个端点：`POST /api/chat/undo`、`POST /api/chat/confirm`
- 会话作用域新增 `global`（全局助手），不再必须依附于某条笔记或某个主题
- 助手入口提升为全局：所有页面右下角都可唤起，当前笔记/主题降级为面板顶部**可摘除的上下文附件**，摘掉后助手面向整个知识库
- 操作卡片：助手每执行一步都在对话里留下一张卡片，写操作可逐条撤销；笔记在此期间被手动改动过则按钮置灰并说明原因
- 删除确认卡片：「允许 / 拒绝」两个按钮，卡片上写明要删的笔记**标题**（而非 id），拒绝后助手会接着回应
- 引用溯源：助手标注的 `[^noteId]` 渲染成可跳转的上标链接；模型编造的 id 保持为纯文本，不生成打不开的死链
- 模型不支持工具调用时，面板顶部提示已降级为纯问答；从未探测过则提示去设置页测一次连接

### 修复

- 设置页「AI 服务」两组配置形态不一致：视觉模型的字段顺序原为「模型 → 接入点 → API Key」，现与文本模型统一为「接入点 → 模型 → API Key」；并补齐每个字段的来源徽标（新增「回落文本模型」一态，与「未配置」区分）与「测试连接」按钮
- 体验模式下 AI 对话没有任何回复：内置 mock LLM 不处理 `stream: true`，始终返回非流式 JSON，导致 SSE 解析读不到内容（表现为消息发出后 AI 毫无反应，且不报错）
- 每日清扫会把全局助手的会话连同撤销载荷一并删除：这类会话没有作用域对象（`scopeId` 为空串），被孤儿判定误伤
- `fetch_url` 误拒用户给过的网址：URL 后紧跟中文（`https://a.com/x这个网址的内容`）时，汉字被吞进 URL，白名单比对落空，助手回「未在本次对话中出现」。改为只按 RFC 3986 的 ASCII 字符提取
- `fetch_url` 在使用代理的机器上完全不可用：`198.18.0.0/15` 原被当作 RFC 2544 基准测试网段拦截，而 Clash / sing-box / Surge 的 TUN 模式默认拿它做 fake-IP，导致每个域名都被判为内网地址
- 助手思考时界面像卡死：等待首个字符只有一个静态省略号，现改为动态字符指示，工具执行中与流式全程都有进行中的信号

### 变更

- `/api/chat` 重构为工具调用循环：单次对话最多 8 轮、每轮最多 20 个调用，工具结果的回灌总量限制在 8000 字符（超出时省略更早的结果）
- SSE 事件在 `{delta}` `{done}` `{error}` 之外新增 `{tool_start}` `{tool_end}` `{confirm_required}`
- 会话列表不再按作用域切分，改为按最近更新倒序列出全部（上限 100 条），旧会话标注「围绕 XX」。`GET /api/chat/conversations` 相应不再需要 `scopeType`/`scopeId` 参数
- mock LLM 支持流式响应与工具调用，并新增 AI 对话与读图的模拟回复；原先对话请求会落到兜底分支，只回一句 `{"pong":true}`

### 安全

- `fetch_url` 只能抓取用户消息中出现过的网址；网页内容里出现的链接不进白名单，切断「模型被网页策反后把知识库内容拼进 URL 发出去」这条外泄路径
- 限制单轮工具调用数为 20 个，避免提示注入把助手的写操作放大成批量污染

## [0.2.0] - 2026-08-14

### 新增

- 回收站：删除笔记改为移入回收站，30 天内可恢复（恢复时搜索索引重建、被打断的 AI 整理自动续跑）；到期随每日清扫彻底删除。入口在 设置 → 数据
- 导出全部数据：设置页一键下载 zip——`主题名/标题-id.md` 目录结构，frontmatter 携带主题/标签/时间等元数据，图片集中在 `assets/` 且正文引用已改写为相对路径，可直接导入 Obsidian 等工具
- 设置页「立即备份」按钮与最近备份时间展示（复用每日备份逻辑，连点自动合并）

### 变更

- 删除语义：物理删除 → 移入回收站；「彻底删除」仅在回收站内可执行
- 孤儿数据随每日清扫回收：无笔记引用的图片（含历史遗留）、失去归属对象的 AI 会话；清扫只在当天备份成功后执行（先备份后销毁）
- 彻底删除的数据在既有备份中最多再保留 7 天（备份轮转所致），此后不可找回

## [0.1.1] - 2026-08-13

### 新增

- 一键体验模式：`npm run demo`（或 `docker compose -f docker-compose.demo.yml up -d`），内置演示数据与本地 mock LLM，无需 API Key 即可体验「随手记 → AI 自动归档 → 主题建议」完整流程
- 健康检查端点 `/api/healthz`；Docker 镜像内置 `HEALTHCHECK`，`docker ps` 可直接看到 healthy 状态
- 每日备份纳入图片目录 `uploads/`（与数据库快照一样保留最近 7 份）；新增文档《[备份与恢复](docs/备份与恢复.md)》
- Vitest 单元测试基线（搜索分词/FTS、标签、AI 流水线、LLM 容错、会话），CI 加入测试步骤
- 发布工作流：推送版本 tag 自动构建多架构（amd64/arm64）镜像发布至 `ghcr.io/b-tech-hub/zhiliao`，并自动创建 GitHub Release
- 社区文件：CONTRIBUTING、SECURITY、Issue/PR 模板
- 英文快速上手 `README.en.md`

### 变更

- `docker-compose.yml` 默认使用预构建镜像（保留本地构建回退方式）
- README 重构：一键体验入口、预构建镜像部署为主路径

## [0.1.0] - 2026-08-13

### 新增

- 首个公开版本：随手记 Markdown 笔记，AI 自动生成标题/标签/摘要并归档到主题
- 低置信度笔记进入「未分类」收集箱，累积后 AI 聚类建议新主题
- 中文全文搜索（jieba 分词 + SQLite FTS5）
- 围绕笔记/主题的 AI 对话（SSE 流式，可选视觉模型读图）
- PWA、深浅色主题、图片上传
- 单密码登录 + JWT 会话；Docker 部署；每日自动备份数据库

[未发布]: https://github.com/B-tech-hub/zhiliao/compare/v0.5.0...HEAD
[0.5.0]: https://github.com/B-tech-hub/zhiliao/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/B-tech-hub/zhiliao/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/B-tech-hub/zhiliao/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/B-tech-hub/zhiliao/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/B-tech-hub/zhiliao/compare/v0.1.1...v0.2.0
[0.1.1]: https://github.com/B-tech-hub/zhiliao/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/B-tech-hub/zhiliao/releases/tag/v0.1.0
