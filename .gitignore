# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# local data (SQLite 数据库与上传图片)
/data/

# 一键体验模式的演示数据（npm run demo）
/data-demo/

# env
.env
.env.local
.env*.local
# 任何 .env 的衍生文件都可能含明文 Key：备份、时间戳副本、临时导出。
# 只写 .env 挡不住 .env.bak-20260819 这类文件名，兜底用通配。
# 例外是 .env.example（不含真实值），下面显式放行。
.env*
!.env.example

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# playwright mcp 产物
.playwright-mcp/

# 备用构建目录（NEXT_DIST_DIR 规避 Windows 句柄锁死时的产物）
.next-verify/

# 本地 AI 协作工具配置（含本机权限白名单等，不进仓库）
.claude/
.spec-workflow/

# 本地头脑风暴与工具链草稿，不进仓库
brainstorm/

# 隔离验收实例的数据目录（端口 3100 那套，不进仓库）
/data-demo-ui/
